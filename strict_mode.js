// VI. Strict Mode
// write the string "use strict " at the ever top of js file
"use strict";

let isAdult = true;
let letRegisteration;
isAdult ? (letRegiteration = true) : (letRegisteration = false);
console.log(letRegisteration);
// num = 10; //missing let or const keyword to declare  a variable
// console.log(num);
